# IAM Defender — Python dependencies
# Install with: pip install -r requirements.txt

# Web server
flask==3.0.3
python-dotenv==1.0.1   # auto-loads .env for local development
# gunicorn==22.0.0  # Linux/production only — omit on Windows

# PDF report generation
reportlab==4.2.2

# AWS SDK (live fetch + IAM policy parsing)
boto3==1.34.131
botocore==1.34.131
